GitHub Actions Pipeline
Steps

Checkout code

Run unit tests

Build backend JAR

Build Docker images

Push images to Docker Hub

Deploy to Kubernetes cluster

Trigger

On push to main

On pull request